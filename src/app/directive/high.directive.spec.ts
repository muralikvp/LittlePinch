import { HighDirective } from './high.directive';

describe('HighDirective', () => {
  it('should create an instance', () => {
    const directive = new HighDirective();
    expect(directive).toBeTruthy();
  });
});
